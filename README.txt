struct Data;
struct OtherData;

parser:
    arr []
    arr.push(header)
    reader.get_line()
        arr.push(str to ,)


introspection:
    foreach elements in line
        if type.has(header[i]) type[header[i]] = data[i]

controller:
    if(digimon.is_selected)
        print_all_moves_which_have_following_attribute(digimon.attribue)
